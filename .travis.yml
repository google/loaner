dist: trusty
addons:
  chrome: stable
  apt:
    sources:
      - google-chrome
    packages:
      - bc
      - google-chrome-stable
      - wget

matrix:
  include:
    - language: python
      python:
        - "2.7"
      before_install:
        # Install bazel.
        - wget https://github.com/bazelbuild/bazel/releases/download/0.24.1/bazel_0.24.1-linux-x86_64.deb
        - echo b6fec7719046953f8b9b18bb798b8d11a84147b936c31e4cfe8d1e6184cc24ed  bazel_0.24.1-linux-x86_64.deb | sha256sum -c
        - sudo dpkg -i bazel_0.24.1-linux-x86_64.deb
        - rm bazel_0.24.1-linux-x86_64.deb
      script:
        - ./backend_tests.sh $GROUP $TOTAL_GROUPS
      env:
        - GROUP=0
        - GROUP=1
        - GROUP=2
        - GROUP=3
        - GROUP=4
    - language: node_js
      node_js:
        - "8.11.0"
      before_install:
        # Enabled Google Chrome Headless.
        - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &
      before_script:
        # This is due to https://github.com/travis-ci/travis-ci/issues/9024
        - "sudo chown root /opt/google/chrome/chrome-sandbox"
        - "sudo chmod 4755 /opt/google/chrome/chrome-sandbox"
      script:
        - ./angular_tests.sh

env:
  global:
    - TOTAL_GROUPS=5
